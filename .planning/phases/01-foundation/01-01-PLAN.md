---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config.py
  - tests/run_layer1.py
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Supabase client is configured using environment variables rather than direct PostgreSQL drivers."
    - "All environment variables must be loaded using load_dotenv with override check."
  artifacts:
    - "config.py exists and handles configurations."
    - "tests/run_layer1.py exists and verifies database connections."
  key_links:
    - "[config.py](file:///c:/Users/ash74/OneDrive/Desktop/Polymarket/config.py)"
    - "[run_layer1.py](file:///c:/Users/ash74/OneDrive/Desktop/Polymarket/tests/run_layer1.py)"
---

# Phase 1 Plan 01: Database Foundation & Environment Setup

## Objective
- **What:** Configure environment variable loading, build the configuration module `config.py`, establish connection tests, and verify all 8 Supabase database schemas.
- **Why:** Provide a clean, robust, and validated database structure and configuration system to bootstrap the trading agent.
- **Output:** Fully functional `config.py` and `tests/run_layer1.py`.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

## Tasks

<task type="code">
  <name>Configure Environment Loading and config.py</name>
  <files>
    - config.py
  </files>
  <action>
    Create config.py to load environment variables (.env / .env.test) safely. Enforce presence of required credentials (SUPABASE_URL, SUPABASE_KEY, etc.) and raise errors on placeholders.
  </action>
  <verify>
    Execute Python to import config and check that missing vars raise errors.
  </verify>
  <done>
    Environment configuration validation is complete.
  </done>
</task>

<task type="code">
  <name>Establish Supabase Schemas and verification tests</name>
  <files>
    - tests/run_layer1.py
  </files>
  <action>
    Create tests/run_layer1.py to connect to Supabase, query all 8 required tables (open_positions, closed_trades, agent_memory, etc.), and verify column definitions and constraint types.
  </action>
  <verify>
    Run `python tests/run_layer1.py` and confirm all checks pass.
  </verify>
  <done>
    Database connection and schema validations pass.
  </done>
</task>

## Verification
- Run the layer 1 test script:
  ```bash
  python tests/run_layer1.py
  ```

## Success Criteria
- [x] All 8 Supabase tables created with schemas matching PLAN.md exactly.
- [x] Python client establishes a connection to the Supabase database.
- [x] Verification test script reports clean validation.
