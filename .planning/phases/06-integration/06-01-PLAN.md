---
phase: 06-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - coordinator/pipeline.py
  - llm/coordinator.py
  - tests/test_integration.py
autonomous: true
user_setup: []
must_haves:
  truths:
    - "All trade decisions must run through the pure-Python risk manager."
    - "Idempotency checks must occur before every Polymarket order submission."
  artifacts:
    - "coordinator/pipeline.py exists and exports routing and coordination logic."
    - "tests/test_integration.py exists and runs clean."
  key_links:
    - "[pipeline.py](file:///c:/Users/ash74/OneDrive/Desktop/Polymarket/coordinator/pipeline.py)"
    - "[test_integration.py](file:///c:/Users/ash74/OneDrive/Desktop/Polymarket/tests/test_integration.py)"
---

# Phase 6 Plan 01: End-to-End Integration & Routing

## Objective
- **What:** Integrate all layers into a unified, dual-path prediction trading pipeline with robust provider and database fail-safes.
- **Why:** Provide a fast, reliable, and secure environment for processing signal decisions and submitting orders safely to the blockchain.
- **Output:** Fully functional integration endpoints (`coordinator/pipeline.py`) and a mock-based test suite (`tests/test_integration.py`).

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

## Tasks

<task type="code">
  <name>Implement Dual-Path Routing Logic</name>
  <files>
    - coordinator/pipeline.py
  </files>
  <action>
    Build fast-path routing (confidence > 0.87, cache hit < 24h, entity match) bypassing deep LLM calls, and full-pipeline routing (news analyst -> contract parser -> trade decision -> risk checks -> coordinator) for all other signals. Ensure both paths call risk_engine.py.
  </action>
  <verify>
    Run integration tests and confirm fast path runs under 5s, full pipeline runs under 22s.
  </verify>
  <done>
    Dual-path routing executes correctly with logged path selections.
  </done>
</task>

<task type="code">
  <name>Integrate Memory warning blocks</name>
  <files>
    - coordinator/pipeline.py
  </files>
  <action>
    Retrieve relevant episodic lessons from agent_memory and prepend them as a marked warning block at the top of the Trade Decision prompt.
  </action>
  <verify>
    Verify prompt content prior to model execution.
  </verify>
  <done>
    Memory warnings are correctly formatted and positioned.
  </done>
</task>

<task type="code">
  <name>Conflict Escalation & LLM Coordinator</name>
  <files>
    - llm/coordinator.py
  </files>
  <action>
    Implement escalation logic firing the LLM Coordinator when News Analyst and Trade Decision disagree on direction and News Analyst confidence is > 0.70. Otherwise, use Python weighted aggregation.
  </action>
  <verify>
    Run mock tests for both high and low News Analyst confidence disagreement scenarios.
  </verify>
  <done>
    Escalation rules function as mathematically specified.
  </done>
</task>

<task type="code">
  <name>Supabase Read Timeout & Fallbacks</name>
  <files>
    - coordinator/pipeline.py
  </files>
  <action>
    Wrap all Supabase queries in 2-second timeout checks (`asyncio.wait_for`) and implement the specified fallbacks: cache fallback to full pipeline, memory fallback to was_memoryless=true, idempotency fallback to fail-closed.
  </action>
  <verify>
    Mock Supabase delays >2.5s and confirm fallback paths execute without crash.
  </verify>
  <done>
    Supabase operations degrade gracefully on timeouts.
  </done>
</task>

<task type="code">
  <name>SiliconFlow Failover to OpenRouter</name>
  <files>
    - coordinator/pipeline.py
  </files>
  <action>
    Add a strict 18-second timeout wrapper on SiliconFlow Trade Decision requests. Fail over immediately to OpenRouter with the identical prompt on timeout.
  </action>
  <verify>
    Mock 19-second SiliconFlow delay and confirm OpenRouter is queried.
  </verify>
  <done>
    LLM API failover operates seamlessly.
  </done>
</task>

<task type="code">
  <name>Order Idempotency Flow</name>
  <files>
    - coordinator/pipeline.py
  </files>
  <action>
    Generate unique UUID at decision time and write it to idempotency_log as pending before order submission. Block duplicates.
  </action>
  <verify>
    Simulate duplicate order execution and check for block.
  </verify>
  <done>
    Idempotency flow prevents double order placement.
  </done>
</task>

## Verification
- Run the pytest integration suite:
  ```bash
  pytest tests/test_integration.py
  ```

## Success Criteria
- [x] Fast path completes in under 5 seconds; full pipeline in under 22 seconds.
- [x] Memory warnings prepended on all Trade Decision calls.
- [x] Disagreements trigger LLM coordinator on high confidence.
- [x] Supabase read timeouts fall back correctly without crashing.
- [x] Failover from SiliconFlow to OpenRouter operates successfully.
- [x] Idempotency UUID prevents duplicate orders.
