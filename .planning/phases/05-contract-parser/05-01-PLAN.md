---
phase: 05-contract-parser
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - llm/contract_parser.py
  - tests/test_parser.py
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Zero LLM imports in risk_engine.py is fully preserved."
    - "The Contract Parser uses DeepSeek V3 via OpenRouter and is not in hot path."
  artifacts:
    - "llm/contract_parser.py exists and exports parser functions."
    - "tests/test_parser.py exists and runs clean."
  key_links:
    - "[contract_parser.py](file:///c:/Users/ash74/OneDrive/Desktop/Polymarket/llm/contract_parser.py)"
    - "[test_parser.py](file:///c:/Users/ash74/OneDrive/Desktop/Polymarket/tests/test_parser.py)"
---

# Phase 5 Plan 01: DeepSeek V3 Contract Parser & Caching

## Objective
- **What:** Build and verify the Contract Parser Agent using DeepSeek V3 via OpenRouter, caching resolution keywords in Supabase with a 24-hour TTL.
- **Why:** Accelerate velocity triggering and bypass heavy LLM runs by caching structured resolution criteria for fast-path signal matching.
- **Output:** Fully functional `/llm/contract_parser.py` and comprehensive test suite `/tests/test_parser.py`.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

## Tasks

<task type="code">
  <name>Implement DeepSeek V3 Contract Parser</name>
  <files>
    - llm/contract_parser.py
  </files>
  <action>
    Implement structured JSON extraction of Polymarket resolution criteria using DeepSeek V3 via OpenRouter. Wrap API calls with an 18-second timeout, strip punctuation/stopwords from keywords, and handle exceptions gracefully by returning None.
  </action>
  <verify>
    Call the parser module manually with a real Polymarket market ID and verify JSON schema returns.
  </verify>
  <done>
    DeepSeek V3 returns the exact 6-field structured JSON format specified in PLAN.md.
  </done>
</task>

<task type="code">
  <name>Implement Supabase Caching & TTL</name>
  <files>
    - llm/contract_parser.py
  </files>
  <action>
    Integrate `resolution_keyword_cache` database caching. Query the table first, wrap all Supabase operations in 2-second timeout wrappers (`asyncio.wait_for`), enforce a 24-hour TTL check, and upsert fresh parsed results.
  </action>
  <verify>
    Perform two consecutive parser runs for a market and confirm the second runs synchronously from the database cache under 2 seconds.
  </verify>
  <done>
    Cached entries are read, stale entries (>24h) trigger background refresh, and db operations fail closed on timeouts.
  </done>
</task>

<task type="test">
  <name>Implement Parser & Cache Unit Tests</name>
  <files>
    - tests/test_parser.py
  </files>
  <action>
    Write comprehensive pytest unit tests in `/tests/test_parser.py` mocking out OpenRouter completions. Verify 6-field JSON formats, 18-second timeout wrappers, 2-second database timeouts, and 24-hour TTL expiration.
  </action>
  <verify>
    Run pytest against the test file: `pytest tests/test_parser.py`
  </verify>
  <done>
    Pytest completes successfully with 100% of parser and cache unit tests passing.
  </done>
</task>

## Verification
- Run the full test suite using pytest to verify parsing and caching accuracy:
  ```bash
  pytest tests/test_parser.py
  ```

## Success Criteria
- [x] DeepSeek V3 OpenRouter API integration complete with 18-second timeout.
- [x] Database cache lookup works with 2-second timeout and 24-hour TTL check.
- [x] Unit tests mock API calls and verify caching, timeout, and refresh behaviors.
