---
phase: 03-calibration-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - strategies/calibration.py
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Novel event categories must fall back to global curves and not exceed 0.50 probability."
    - "Brier score computation must match exact probability metrics."
  artifacts:
    - "strategies/calibration.py exists and exports CalibrationModel and edge logic."
  key_links:
    - "[calibration.py](file:///c:/Users/ash74/OneDrive/Desktop/Polymarket/strategies/calibration.py)"
---

# Phase 3 Plan 01: Statistical Calibration Curves & Edge Logic

## Objective
- **What:** Create the probability calibration engine and Brier score tracking logic.
- **Why:** Correct subjective LLM estimates using historical performance metrics and accurately flag mispriced contracts (edge >= 7 cents).
- **Output:** Fully functional `strategies/calibration.py`.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

## Tasks

<task type="code">
  <name>Build Calibration Model and Brier Score</name>
  <files>
    - strategies/calibration.py
  </files>
  <action>
    Create calibration.py to load calibration data. Implement Brier score formula tracking. Create separate calibration curves for politics, crypto, sports, science, legal, and economics, fallback to global curve, and limit unknown category probabilities to <= 0.50.
  </action>
  <verify>
    Execute python tests verifying the Brier score returns 0.025 on the standard benchmark data.
  </verify>
  <done>
    Brier score calculations are correct and fallback thresholds are enforced.
  </done>
</task>

<task type="code">
  <name>Implement Edge Flagging Logic</name>
  <files>
    - strategies/calibration.py
  </files>
  <action>
    Implement probability difference edge calculations: |agent_estimate - market_price|. Flag markets as mispriced if the edge is >= 7 cents; ignore markets below this threshold.
  </action>
  <verify>
    Call edge logic with synthetic inputs: 13-cent edge flags, 4-cent edge does not flag.
  </verify>
  <done>
    Edge calculations correctly distinguish tradeable versus non-tradeable opportunities.
  </done>
</task>

## Verification
- Run tests verifying calibration:
  ```bash
  pytest tests/test_risk.py -k "calibration"
  ```

## Success Criteria
- [x] Brier score computation is mathematically correct (returns 0.025 exactly).
- [x] Calibration engine flags edge >= 7 cents and skips edge < 7 cents.
- [x] Category calibration curves exist.
- [x] Novel categories fall back to global curve and limit confidence to <= 0.50.
