---
phase: 02-data-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/rss_poller.py
  - data/spacy_filter.py
autonomous: true
user_setup: []
must_haves:
  truths:
    - "GDELT feed must not be used on the velocity/fast path due to structural lag."
    - "RSS feeds must poll at intervals under or equal to 10 seconds."
  artifacts:
    - "data/rss_poller.py exists and exports RSS polling loops."
    - "data/spacy_filter.py exists and exports pre-filter mechanisms."
  key_links:
    - "[rss_poller.py](file:///c:/Users/ash74/OneDrive/Desktop/Polymarket/data/rss_poller.py)"
    - "[spacy_filter.py](file:///c:/Users/ash74/OneDrive/Desktop/Polymarket/data/spacy_filter.py)"
---

# Phase 2 Plan 01: RSS Feed Poller & NLP Prefilter

## Objective
- **What:** Develop the high-frequency RSS Poller and the spaCy-based natural language pre-filter.
- **Why:** Enable low-latency ingestion of news events and pre-validate headlines to exclude noise before calling expensive LLMs.
- **Output:** Fully functional `data/rss_poller.py` and `data/spacy_filter.py`.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

## Tasks

<task type="code">
  <name>Build RSS feed poller</name>
  <files>
    - data/rss_poller.py
  </files>
  <action>
    Create rss_poller.py to connect to 20+ feeds (AP, Reuters, Federal Reserve, SCOTUS, etc.) with a configurable polling interval of 10 seconds or less.
  </action>
  <verify>
    Execute the poller module and verify that it fetches feeds without network exceptions.
  </verify>
  <done>
    Feeds are polled successfully in less than 10-second cycles.
  </done>
</task>

<task type="code">
  <name>Build spaCy pre-filter</name>
  <files>
    - data/spacy_filter.py
  </files>
  <action>
    Create spacy_filter.py to load 'en_core_web_md' or similar and filter out non-market-impacting headlines. Include a domain allowlist to immediately bypass NLP checks for specific market keywords.
  </action>
  <verify>
    Verify allowlist and spaCy filters with synthetic headlines (finance/politics allowed, spam blocked).
  </verify>
  <done>
    Pre-filter classifies headlines accurately andallowlist routing works.
  </done>
</task>

## Verification
- Run individual modules to check performance:
  ```bash
  python data/spacy_filter.py
  ```

## Success Criteria
- [x] RSS poller accesses feeds in <= 10 seconds.
- [x] spaCy pre-filter blocks irrelevant headlines.
- [x] Domain allowlist bypasses spaCy pre-filter checks correctly.
