---
phase: 02-data-pipeline
plan: 02
type: execute
wave: 1
depends_on:
  - 02-01
files_modified:
  - llm/news_analyst.py
  - data/pipeline.py
autonomous: true
user_setup: []
must_haves:
  truths:
    - "News Analyst calls must wrap APIs with a strict 10-second timeout."
    - "Signals with confidence score below 0.75 must be discarded and logged."
  artifacts:
    - "llm/news_analyst.py exists and exports classify_signal."
    - "data/pipeline.py exists and exports run_pipeline."
  key_links:
    - "[news_analyst.py](file:///c:/Users/ash74/OneDrive/Desktop/Polymarket/llm/news_analyst.py)"
    - "[pipeline.py](file:///c:/Users/ash74/OneDrive/Desktop/Polymarket/data/pipeline.py)"
---

# Phase 2 Plan 02: News Analyst & Pipeline Ingestion Queue

## Objective
- **What:** Build the News Analyst LLM agent and coordinate feed ingestion via an asynchronous queue and concurrent worker loop.
- **Why:** Perform structured classification of event signals in real time while maintaining processing velocity.
- **Output:** Fully functional `llm/news_analyst.py` and `data/pipeline.py`.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

## Tasks

<task type="code">
  <name>Implement News Analyst Agent</name>
  <files>
    - llm/news_analyst.py
  </files>
  <action>
    Create news_analyst.py using Qwen3-32B via OpenRouter. Return structured JSON output (event_category, affected_market_ids, confidence_score, direction). Wrap calls in a 10s timeout, dropping the signal gracefully if timed out.
  </action>
  <verify>
    Call classify_signal with a sample headline and verify structured JSON returns.
  </verify>
  <done>
    News Analyst returns correct schema and handles timeouts gracefully.
  </done>
</task>

<task type="code">
  <name>Implement Pipeline Queue and Workers</name>
  <files>
    - data/pipeline.py
  </files>
  <action>
    Create pipeline.py with run_pipeline, start 3 concurrent workers, process inputs using asyncio queue. Pushes verified signals to market_signals table and discards low-confidence (<0.75) inputs.
  </action>
  <verify>
    Run the pipeline with a stream of 10 signals and verify Supabase entries.
  </verify>
  <done>
    Ingestion pipeline handles concurrent workers and logs correctly.
  </done>
</task>

## Verification
- Confirm that unit tests for news analyst and pipeline run:
  ```bash
  pytest tests/test_alerts.py
  ```

## Success Criteria
- [x] News Analyst returns category, market IDs, confidence, and direction.
- [x] OpenRouter 10-second timeout behaves correctly.
- [x] Low confidence signals are logged as discarded.
- [x] Ingestion queue processes signals concurrently using 3 workers.
